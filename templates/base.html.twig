<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets%}
    <link rel="stylesheet" href="/stylesheets/screen.css ">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Hello, world!</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" rel="stylesheet">{% endblock %} {% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous">
    </script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> {% endblock %}
</head>
<style>
    nav {
        background-color: #1c3c58;
    }
</style>

<body>
    {% block menu %}
    <nav class="navbar navbar-expand-lg 
                navbar-dark">

        <a class="navbar-brand" href="{{path('catalogue_burger')}}"><i class='fa-solid fa-burger' style='color: white'></i></i></a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto dis">
                <li class="nav-item active">
                    <a class="nav-link" href="#menu">Menu</a>
                </li>
                <li class="nav-item sdf">
                    <a class="nav-link" href="#burger">Burger</a>
                </li>
                {% if app.user%} {% if is_granted('ROLE_USER') %}
                <div class="dropdown first">
                    <button class="dropbtn">Mes commandes</button>
                    <div class="dropdown-content">
                        <a href="#">Faire une commande</a>
                        <a href="{{path('app_commande')}}">Lister mes commandes</a>
                    </div>
                </div>
                {% endif %} {% endif %} {% if app.user %}{% if is_granted('ROLE_GESTIONNAIRE') %}
                <div class="dropdown second">
                    <button class="dropbtn">Faire Ajout</button>
                    <div class="dropdown-content">
                        <a href="{{path('add_burger')}}">Ajouter burger</a>
                        <a href="{{'/menu'}}">Ajouter Menu</a>
                        <a href="{{path('add_complement')}}">Ajouter Complement</a>
                    </div>
                </div>


                <div class="dropdown third">
                    <button class="dropbtn">Lister</button>
                    <div class="dropdown-content">
                        <a class="#" href="{{path('app_lister_burger')}}">Lister burger</a>
                        <a class="#" href="{{path('app_commande_gestionnaire')}}">Lister commande</a>
                        <a class="#" href="{{path('app_lister_menu')}}">Lister Menu</a> {# <a class="#" href="{{path('app_lister')}}">Lister Complement</a> #}
                    </div>
                </div>
                {% endif %} {% endif %} {% if not app.user%}
                <li>
                    <a href="{{path('app_login')}}"><i class='fa fa-shopping-cart' style='color: white'></i></a>
                </li>{% else %}{% if is_granted('ROLE_USER') %}
                <li class="pandeux">
                    <a href="{{path('app_panier')}}"><i class='fa fa-shopping-cart' style='color: white'></i></a>
                </li>{% endif %}{% endif %}
            </ul>
            {% if not app.user %}
            <div class="dropdown drt">
                <button class="dropbtn"><i class="fas fa-user-plus"></i></button>
                <div class="dropdown-content">
                    <a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
                    <a class="nav-link" href="{{path('app_register')}}">S'inscrire</a>
                </div>
            </div>
            {% endif %} {% if app.user %}
            <div class="dropdown drt">
                <button class="dropbtn"><i class="fas fa-user-plus"></i></button>
                <div class="dropdown-content">
                    <a class="nav-link" href="{{path('app_logout')}}">Se deconnecter</a>
                </div>
            </div>
            {% endif %}
        </div>
    </nav>
    <style>
        .dropbtn {
            background-color: #1c3c58;
            color: white;
            padding: 16px;
            font-size: 16px;
            border: none;
        }
        /* The container <div> - needed to position the dropdown content */
        
        .dropdown {
            position: relative;
            display: inline-block;
        }
        /* Dropdown Content (Hidden by Default) */
        
        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f1f1f1;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
            z-index: 1;
        }
        /* Links inside the dropdown */
        
        .dropdown-content a {
            color: black;
            padding: 12px 16px;
            text-decoration: none;
            display: block;
        }
        /* Change color of dropdown links on hover */
        
        .dropdown-content a:hover {
            background-color: #ddd;
        }
        /* Show the dropdown menu on hover */
        
        .dropdown:hover .dropdown-content {
            display: block;
        }
        /* Change the background color of the dropdown button when the dropdown content is shown */
        
        .dropdown:hover .dropbtn {
            background-color: #1c3c58;
        }
        
        .third {
            margin-left: 130px;
        }
        
        .second {
            margin-left: 130px;
        }
        
        .first {
            margin-left: 130px;
        }
        
        .drt {
            margin-right: 100px;
        }
        
        .sdf {
            margin-left: 80px;
            color: #fff;
        }
        
        .fa-burger {
            width: 30%;
        }
        
        .fa {
            margin-left: 920%;
            margin-top: 60%;
        }
        
        .pandeux {
            margin-left: 20%;
        }
    </style>{% endblock %} {% block body %}{% endblock %}
</body>

</html>